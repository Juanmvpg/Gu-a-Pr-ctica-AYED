#include <iostream>
using namespace std;

int main (){
/*GENERAL
    //VARIABLES SIMPLES HEAP
    // int* ptr = new int; //crea un int en el heap
    // int* ptr = new int (42); //crea un int en el heap con valor inicial 42
    
    // *ptr = 10; //se usa, escribe en donde apunta

    //delete ptr; // se libera la memoria
    //ptr=nullptr; //buena práctica



    //VECTORES HEAP
    //int n = 5;
    //int* arr = new int [n]   //crea 5 ints contíguos en el heap
    //int* arr = new int [n] () // los inicializa en 0

    //arr[i]= valor //así se usa normal, en posición i sobreescribe
    // * (arr+i) = valor //así se usa con puntero aritmético, en posición arr+i sobreescribe

    //delete[] arr; //libera todo el bloque
    //arr= nullptr; //buena práctica

    //CRASH
    //TRY
    // int *ptr = nullptr; //ESTO DA ERROR porque
    //*ptr = n; // ESTO ES LO MISMO QUE ESCRIBIR EN NULLPTR, O SEA NO SE PUEDE
    //int *ptr = new int;
    //*ptr = n;
    //cout<< ptr<< endl; //dirección de memoria donde está guardado n
    //cout<< &ptr<< endl; //dirección de memoria donde está guardado el puntero ptr





    //
    
    //ARITMÉTICA DE PUNTEROS
    /*int arr [5] = {10, 20, 30, 40, 50};
    cout << arr<<endl; //imprime la dirección del primer elemento
    cout << &arr [1]<<endl; //imprime la dirección del primer elemento

    cout<< arr [0]<<endl; //imprime valor del primer elemento
    cout<< *arr<< endl; // imprime primer valor
    cout << *(arr + 2) << endl; // imprime tercer valor, permite acceder a la posición i
    */
//





    return 0;
}
